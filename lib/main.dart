import 'package:flutter/material.dart';
import 'package:hm_shop/routes/index.dart';

// -api          #存放请求

// -assets       #存放资源

// -components   #存放公共组件

// -contants     #存放常量文件

// -viewmodels   #存放类型文件

// -pages        #存放页面

// -routes       #存放路由配置

// -stores       #存放全局状态组件

// -utils        #存放工具类

void main() {
  //runapp 启动应用
  runApp(getRootWidget());
}
